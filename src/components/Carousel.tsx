type CarouselProps = {
  images: string[];
};

export function Carousel({ images }: CarouselProps) {
  return null;
}

// going to have to add image paths to projects

// will need state and buttons, which will be number of images